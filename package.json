{
  "name": "sauce-labs-test-automation",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "ui": "playwright test --ui",
    "docker:test": "docker-compose up tests"
  },
   "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}